# HubSpot Private App access token or OAuth access token.
# Create a private app at: Settings → Integrations → Private Apps
# Scopes needed: crm.objects.contacts.read, crm.objects.contacts.write,
#                crm.objects.deals.read, crm.objects.contacts.associations.read,
#                and read/write for engagements (calls, emails, meetings, notes, tasks) if counting them.
HUBSPOT_ACCESS_TOKEN=your-token
